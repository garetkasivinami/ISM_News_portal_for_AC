@model ISMNewsPortal.Models.NewsPostSimplifiedCollection

@{
    ViewBag.Title = "Home Page";
}

<div class="jumbotron">
    <h1>ACME COSMETICS</h1>
</div>

<div class="wrapper">
    <nav class="navbar navbar-expand-sm navbar-light bg-light">
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item dropdown drop-flex">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Filter by...
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        @Html.ActionLink("Today", "Index", "Home", new { @filter = "today", @page = Model.currentPage, @sortType = Model.sortType }, new { @class = "dropdown-item" })
                        @Html.ActionLink("Yesterday", "Index", "Home", new { @filter = "yesterday", @page = Model.currentPage, @sortType = Model.sortType }, new { @class = "dropdown-item" })
                        @Html.ActionLink("This week", "Index", "Home", new { @filter = "week", @page = Model.currentPage, @sortType = Model.sortType }, new { @class = "dropdown-item" })
                        @Html.ActionLink("All", "Index", "Home", new { @page = Model.currentPage, @sortType = Model.sortType }, new { @class = "dropdown-item" })
                    </div>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Sort by...
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        @Html.ActionLink("Name", "Index", "Home", new { sortType = "name", @page = Model.currentPage, @filter = Model.filter }, new { @class = "dropdown-item" })
                        @Html.ActionLink("Description", "Index", "Home", new { sortType = "description", @page = Model.currentPage, @filter = Model.filter }, new { @class = "dropdown-item" })
                        @Html.ActionLink("Date", "Index", "Home", new { }, new { @class = "dropdown-item", @page = Model.currentPage, @filter = Model.filter })
                    </div>
                </li>
            </ul>
            <form class="form-inline my-2 my-lg-0">
                <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
                <button class="btn btn-outline-dark my-2 my-sm-0" type="submit">Search</button>
            </form>
        </div>
    </nav>
    @if (Model.NewsPostSimpliedViews.Any())
    {
        foreach (ISMNewsPortal.Models.NewsPostSimplifiedView newsPost in Model.NewsPostSimpliedViews)
        {
            <div class="card news-item">
                @Html.Partial("~/Views/News/_NewsSimplifyPartial.cshtml", newsPost)
            </div>
        }
    }
    @if (Model.currentPage > 0)
    {
        @Html.ActionLink("Last page", "Index", "Home", new { sortType = Model.sortType, @page = Model.currentPage - 1, @filter = Model.filter }, new { });
    }
    @if (Model.currentPage > 1 && Model.pages > 2)
    {
        @Html.ActionLink("First page", "Index", "Home", new { sortType = Model.sortType, @page = 0, @filter = Model.filter }, new { });
    }
    @if (Model.currentPage < Model.pages - 1)
    {
        @Html.ActionLink("Next page", "Index", "Home", new { sortType = Model.sortType, @page = Model.currentPage + 1, @filter = Model.filter }, new { });
    }
</div>